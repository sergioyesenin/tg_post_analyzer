# event/expert
Generate structured expert observations for the event.

Requirements:
- stay within available evidence and trace limits
- explicitly separate facts from interpretations
- avoid external claims unless retrieval is successful and sourced
- surface uncertainty when data is incomplete

Output contract:
- short machine-readable block of observations
- each observation must be auditable against inputs

