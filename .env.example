# Telegram
TG_API_ID=123456
TG_API_HASH=your_api_hash_here
TG_SESSION_NAME=tg_analytics.session
TG_PHONE=+10000000000
TG_FLOOD_SLEEP_THRESHOLD=5

# App
APP_ENV=dev
APP_TZ=UTC
LOG_LEVEL=INFO
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://localhost:8000,http://127.0.0.1:8000
CORS_ALLOW_CREDENTIALS=true

# Database / docker-compose credentials
POSTGRES_DB=tg_analytics
POSTGRES_USER=tg_analytics_app
POSTGRES_PASSWORD=GQD+uF]G3hnXPeXRub*gJ+M6+aGoX)wn
DB_URL=postgresql+asyncpg://tg_analytics_app:replace-with-strong-password@localhost:5432/tg_analytics

# Linking
LINKING_PIPELINE_VERSION=v2-evidence-first
LINKING_TOP_K=50
LINKING_EMBED_ENABLED=true
LINKING_EMBED_MODEL=nomic-embed-text
LINKING_EMBED_TIMEOUT_SEC=20
LINKING_EMBED_MIN_SIM=0.85
LINKING_PREFILTER_MULTIPLIER=8
LINKING_SAME_EVENT_ENTITY_OVERLAP_MIN=0.2
LINKING_RELATED_TIME_WINDOW_HOURS=168
LINKING_MAX_TEXT_CHARS=5000
NO_LLM_SAME_EVENT_MIN_SIM=0.915
NO_LLM_SAME_EVENT_MAX_HOURS=10

# Optional LLM-backed reporting/titles (outside linking)
LINKER_LLM_MODEL=ollama/llama3:8b-instruct-q4_K_M
LINKER_LLM_BASE_URL=http://localhost:11434
LINKER_LLM_API_KEY=
DISCUSSION_FALLBACK_ID_WINDOW=1
DISCUSSION_FALLBACK_MAX_SECONDS=10

# Auth
# Generate a strong secret, for example:
# python -c "import secrets; print(secrets.token_urlsafe(48))"
AUTH_JWT_SECRET=JGQYMrpLKggJDl174_vUIhZXSkv_EysYkNOpw30vVl_YcuMYtUEe4BcEDkBBgwVy
AUTH_JWT_ALG=HS256
AUTH_ACCESS_TTL_MINUTES=60
AUTH_REFRESH_TTL_DAYS=30
AUTH_PROVIDER_MODE=local
AUTH_REFRESH_COOKIE_NAME=tgpa_refresh
AUTH_REFRESH_COOKIE_SECURE=false
AUTH_REFRESH_COOKIE_SAMESITE=lax
AUTH_REFRESH_COOKIE_DOMAIN=
AUTH_REFRESH_COOKIE_PATH=/api/auth

# Compatibility aliases for one release (deprecated):
# TZ -> APP_TZ
# DATABASE_URL -> DB_URL
